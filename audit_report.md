# 🔍 Технический аудит проекта Ninja-Snatch v9.0

> **Объект анализа:** Chrome Extension для захвата HTML/CSS.  
> **Статус:** Фаза 2 завершена.  
> **Режим анализа:** ULTRATHINK.

---

## 🚩 Введение для специалиста

Этот документ содержит объективную критику текущей архитектуры расширения. Цель — не обесценить текущие достижения, а указать на "бутылочные горлышки" и архитектурные тупики, которые помешают масштабированию проекта до промышленного уровня.

---

## 1. Архитектурная критика: IIFE Monolith

### Текущее состояние
Ядро системы (`styleInjector.js`) реализовано в виде гигантского IIFE (Immediately Invoked Function Expression). 

### Проблемы:
*   **Нарушение принципа единственной ответственности (SRP):** Один файл отвечает за:
    1.  Скрапинг CSSOM.
    2.  Парсинг и очистку DOM.
    3.  Манипуляцию URL.
    4.  Генерацию новых CSS/JS блоков.
*   **Сложность тестирования:** Практически невозможно написать Unit-тесты для отдельных функций, так как они скрыты внутри замыкания IIFE и зависят от глобального состояния `document`.
*   **Отсутствие типов:** Использование чистого JS в проекте такого объема (>600 строк логики) приводит к накоплению скрытых ошибок (runtime errors), которые проявляются только на специфических сайтах.

### Рекомендация:
Переход на **TypeScript** и модульную систему (ES Modules) с использованием сборщика (**Vite** или **esbuild**). Разделение на `Parser`, `Cleaner`, `Formatter` и `AnimationEngine`.

---

## 2. Алгоритмическая критика: Эвристический матчинг CSS

### Текущее состояние
Функция `getMatchedCSSRules` использует метод `selectors.add(el.classList)` и последующий поиск подстроки `rule.selector.includes(class)`.

### Проблемы:
*   **CSS Bloat (Раздувание):** Это самый критичный недостаток. Алгоритм матчит селектор `.btn-primary`, если у элемента есть класс `.btn`. В итоге в экспортируемый файл попадает 50-70% лишнего CSS, который не относится к выбранному узлу.
*   **Сложные селекторы:** Комбинированные селекторы (например, `.nav > ul li.active`) могут некорректно определяться эвристикой, основанной на простых проверках подстроки.
*   **Производительность:** Перебор всех правил (`allCSSRules`) для каждого элемента в цикле имеет квадратичную сложность $O(n^2)$ в худшем случае.

### Рекомендация:
Использование нативного метода `element.matches(selector)`. Это позволит браузеру самому определять применимость правила, гарантируя 100% точность и минимальный размер итогового CSS.

---

## 3. Проблема производительности: Синхронный скрапинг

### Текущее состояние
Метод `collectAllCSS` сканирует все `document.styleSheets` синхронно при каждом нажатии кнопки "Grab".

### Проблемы:
*   **Main Thread Blocking:** На тяжелых сайтах (например, порталах с 10+ внешними CSS файлами) этот процесс занимает 200-500мс, полностью блокируя поток интерфейса (Jank).
*   **Отсутствие кеширования:** Расширение не кеширует результаты первичного сканирования. Если пользователь копирует 5 элементов подряд, расширение 5 раз выполняет одну и ту же тяжелую работу по парсингу всех стилей страницы.

### Рекомендация:
Реализовать фоновое кеширование стилей при загрузке страницы и использовать **Web Workers** для тяжелых операций форматирования (prettifyHTML).

---

## 4. Отношение к Animation Runtime

### Текущее состояние
Проект инжектит `motion` (библиотеку) через внешний CDN и накладывает свои скрипты поверх существующих.

### Проблемы:
*   **Конфликты библиотек:** Если на сайте уже есть `framer-motion` другой версии, возможны конфликты в глобальной области видимости.
*   **CSP (Content Security Policy):** Многие сайты блокируют импорт скриптов с внешних CDN (`jsdelivr.net`). В таких случаях анимации просто не запустятся.

### Рекомендация:
Включать runtime анимаций в бандл расширения или использовать чистые CSS-анимации там, где это возможно, минимизируя зависимость от внешних JS-движков.

---

## 5. Итоговая оценка (Verdict)

*   **Текущий уровень качества:** **B- (Рабочий прототип / MVP+)**
*   **Готовность к масштабированию:** **Низкая.** При добавлении поддержки Shadow DOM или сложных SPA проект потребует полной переписки ядра.
*   **Сильная сторона:** Великолепная работа с URL ассетов и визуальная составляющая (UI расширения и Visual Sniper).

### План модернизации (Roadmap для специалиста):
1.  **Refactor Stage 1:** Внедрение TypeScript + Vite. Превращение IIFE в набор чистых функций.
2.  **Refactor Stage 2:** Замена `includes`-матчинга на нативный `matches`.
3.  **Refactor Stage 3:** Выделение `PATTERNS` в отдельный JSON-конфиг, инжектируемый динамически.
4.  **Refactor Stage 4:** Реализация поддержки Shadow DOM через рекурсивный обход `shadowRoot`.

---
*Отчет сформирован: 2025-12-22*
